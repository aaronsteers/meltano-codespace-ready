{
    "name": "Codespaces Meltano CLI Starter",
    "extensions": [
        "z3z1ma.meltano-power-user",
        "redhat.vscode-yaml",
    ],
    "postCreateCommand": "meltano --version",
    "dockerFile": "Dockerfile",
    "settings": {
    },
    "features": {
        "docker-in-docker": {
            "version": "latest",
            "moby": true,
            "dockerDashComposeVersion": "v1"
        }
    }
}
